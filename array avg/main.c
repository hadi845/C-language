#include<stdio.h>
void main()
{
    int array[10]={2,6,9,10,40,38,21,4,15,44};
    int sum=0,avg;
    for(int cacacaci=0;i<=9;i++)
    {
        sum=sum+array[i];
    }
    avg=sum/10;
    printf(" average of the array elements = %d",avg);
}
